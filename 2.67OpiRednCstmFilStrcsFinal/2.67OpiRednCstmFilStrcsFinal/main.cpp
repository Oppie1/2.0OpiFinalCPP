#include <iostream>
#include <fstream>
using namespace std;

//Must create file to read from. Just use molts.txt from GitHub repo via solution explorer source files.

int main() {

	//Instantiate object named adamFile of ifstream class with parameter "molts.txt"
	//CODE:

	//Declare three vars for id, name and money designating data type int, string , double.
	//CODE:

	//Create while loop that extracts information from adamFile object (id, name, and money).
	//CODE:

	//Output that's formatted to support contents of file.
	//CODE:

}