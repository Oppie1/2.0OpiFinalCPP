#include<iostream>
using namespace std;


int main() {


	//Declare two int initialized to 17 and 47.
	//CODE:

	//Use if statement with less than operator and output statement if condition is true.
	//CODE:
	

	//Use if statement with less than/equal to operator and output message if true.
	//CODE:

	//Use if statement that is false with message in code block that will not print because if is false.
	//CODE:

	//Use a this value "equals" that value (==)..
	//CODE:

	//Use if condition with "!=" with values 5 and 8. Then output complementary message.
	//CODE:

	//Use actual number values  5 < 3 in if condition with message in codeblock (will not execute ).
	//CODE:
	
	//Use actual numbers 3 <= 3 in if condition with message in codeblock (will execute).
	//CODE:

}