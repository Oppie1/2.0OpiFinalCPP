#include <iostream>
using namespace std;




int main() {

	//Declare uninitialized int var age.
	//CODE:

	//Declare int var named tokens initialized to 47.
	//CODE:

	cout << "\nYou must be at least be 29 years old or have 40 or more tokens to enter online tradeshow.\n" << endl;

	//Output prompting user to enter an age.
	//CODE:
	cout << "Please enter your age." << endl;

	//Input user's info.
	//CODE:

	//Use if condition comparing age to 33. If age is greater than 33 person can enter tradeshow
	//with "or" operator and another condition tokens is greater than or equal to 40.
	//CODE:

	//Output statement in codeblock displaying to screen that user is allowed in tradeshow.
	//CODE:

	//Use else to output to screen user could can not enter tradeshow because age or tokens.
	//CODE:	

	//CODE:
	cout << "Either if or else/if executed the program goes to next step in main() which is just this cout statement." << endl;
}