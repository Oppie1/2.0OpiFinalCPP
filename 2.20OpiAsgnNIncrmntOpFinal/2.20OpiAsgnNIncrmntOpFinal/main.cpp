#include <iostream>
using namespace std;




int main()
{

	//Declare and initialize 3 int vars x, y and z to 20, 30 and 10 respectively.
	//CODE:

	//Example of increment operator "++" (after or before var in expression).
	cout << "Example of increment operator in EXPRESSION that ++ after x -> x++ so x still = \n";

	//Output x++ to screen to see effect the order of the increment operator has.
	//CODE:

	cout << "Now the 'x' has been incremented in operation above x++ and so has increased by one:\n";

	//Output statement displaying x to the screen to see new value of x.
	//CODE:

	//Output sentence and y to screen incremented before var.
	//CODE:	

	cout << "\n\nThe modulus operator is '%' expression is saying divide by but only give the remainder - here we use a variable 'z'\n";
	cout << "But we can use a value alone (second example)\n";

	//Output modulus "z" by 3 to screen.
	//CODE:

	//Output line of code where we use the number 10 % 3 to screen.
	//CODE:

}