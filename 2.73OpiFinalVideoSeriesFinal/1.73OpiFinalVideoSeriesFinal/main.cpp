#include <iostream>
#include <string>
using namespace std;



int main() {


	//Instantiate string object named s1 of string class with sentence about your 
	//understanding which is basically nothing.
	//CODE:

	//Output to show s1.
	//CODE:

	//Use replace() to take out word in sentence and replace it with "OPPIE" and output to screen.
	//s1.replace() contains 3 parameters. 1. How many elements to count 2. followed by how many
	//elements of substring we are replacing 3. And what to replace those elements/characters with.
	//CODE:

	//Call replace() again with arguments 17, 4,"KANOPI" on the s1 object. Then output new
	//sentence to the screen (s1 object).
	//CODE:

	//insert() contains two parameters. 1. Number of elements
	//to count. 2. What to insert into the string at index location whatever you want.

	//Call the insert() function with arguments with whatever you want.
	//CODE:

	//Call replace() again with arguments 0, 5, "Adam" on s1 object and add
	//output statement to show that sentence (s1 object) to the screen.
	//CODE:
	
}