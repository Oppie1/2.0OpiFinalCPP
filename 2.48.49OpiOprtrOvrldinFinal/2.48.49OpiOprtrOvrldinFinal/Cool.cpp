#include <iostream>
#include "cool.h"
using namespace std;


//Define Adam constructor of Adam class. Initialize member var num to 0 
//using member initializer list.
//CODE:

//Define an Adam function with int var parameter. Then assign that int var parameter value to num.
//CODE:

//Define operator^ function for Adam class. The first Adam is the return type - this function returns a Adam
//OBJECT and takes another Adam object as its parameter. Adam OBJECT = of the Adam CLASS.
//CODE:

//Create a newObject of the Adam class.
//CODE:

//Return with new object as parameter.
//CODE:

//This clarifies that:
//The first Adam = return type class object
//The second Adam:: = class.
//operator^ the overloaded operator
//(Adam obj) = the parameter