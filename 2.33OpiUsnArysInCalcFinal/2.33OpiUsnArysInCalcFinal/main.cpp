#include <iostream>
using namespace std;



int main() {


	//Declare int array named tuna with 5 elements initialized to (5,10,15,20,25).
	//CODE:

	//Declare an integer value named sum initialized to 0.
	//CODE:

	cout << "Below is the total of an array with 5 elements added together as it iterates through indices.\n" << endl;

	//Declare for loop with int value x initialized to 0. Then set control to x is 
	//less than 5 then iterate x by 1.
	//CODE:

	//Assign sum to expression adding elements of array together during each iteration.
	//CODE:

	//Output statement that shows which iteration x program is on and running total to the screen:
	//CODE:

	//Output statement to let the user know the for loop has fully executed and that control returns to main().
	//CODE:
	cout << "\nCongrats for loop has executed fully and now your back in main for next step." << endl;

}